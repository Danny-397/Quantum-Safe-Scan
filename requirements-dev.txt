# Development / test dependencies.
# Install with: pip install -e . && pip install -r backend/requirements.txt && pip install -r requirements-dev.txt
pytest==8.3.3
# Validates SARIF export against the official OASIS JSON Schema (test_conformance.py).
jsonschema>=4.0
