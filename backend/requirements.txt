# QuantumSafe backend
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-Cors==4.0.1
Flask-Limiter==3.8.0
Flask-Mail==0.10.0
bcrypt==4.2.0
python-dotenv==1.0.1
email-validator==2.2.0
itsdangerous==2.2.0
gunicorn==23.0.0
psycopg2-binary==2.9.9

# Shared scanner package (CLI + backend share one detection engine).
# Installed from the repo root; on Render this is handled by the build command:
#   pip install -e . && pip install -r backend/requirements.txt
rich>=13.0
